//DRAW P5
function setup() {
    
    createCanvas(300, 300);
    background(0);
  }
  
  function draw() {
    fill(255,0,40);
    ellipse(100, 100, words.length * 10, words.length * 10);
    words.length
  }